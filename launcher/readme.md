## Requires:

dotnet sdk

python 

`yarn build` in ./trading-dashboard

* todo: add automatic build

## Then


`dotnet run`

* todo: make executable 

## What it does

launches python process via Process.Start()

launches a kestrel server that hosts react-app trading-dashboard:

* http://localhost:3000 